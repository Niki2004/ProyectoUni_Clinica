@model ProyectoClinica.Models.ShowNewPasswordViewModel

@{
    ViewBag.Title = "Recuperación de Contraseña";
}

<style>
    body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background: linear-gradient(to bottom, #b2ebf2, #ffffff);
    }

    main {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        padding: 20px;
    }

    .form-container {
        background-color: white;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        max-width: 600px;
        width: 100%;
        text-align: center;
    }

    h2 {
        margin-bottom: 20px;
        text-align: center;
        color: #0b7b82;
    }

    p {
        margin-bottom: 15px;
        line-height: 1.6;
        color: #333;
    }

    .email-highlight {
        font-weight: bold;
        color: #0b7b82;
    }

    .btn-primary {
        background-color: #0b7b82;
        color: white;
        border: none;
        border-radius: 5px;
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s;
        text-decoration: none;
        display: inline-block;
        margin-top: 20px;
    }

    .btn-primary:hover {
        background-color: #095e63;
    }

    .icon-success {
        font-size: 48px;
        color: #28a745;
        margin-bottom: 20px;
    }
</style>

<main aria-labelledby="title">
    <div class="form-container">
        <div class="icon-success">
            <i class="fas fa-check-circle"></i>
        </div>
        <h2 id="title">@ViewBag.Title</h2>
        <p>Hemos enviado un correo electrónico a <span class="email-highlight">@ViewBag.Email</span> con instrucciones para restablecer tu contraseña.</p>
        <p>Por favor, revisa tu bandeja de entrada y sigue las instrucciones para acceder a tu cuenta.</p>
        <p>Si no recibes el correo en unos minutos, verifica tu carpeta de correo no deseado.</p>
        <a href="@Url.Action("Login", "Account")" class="btn-primary">Ir a iniciar sesión</a>
    </div>
</main>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

