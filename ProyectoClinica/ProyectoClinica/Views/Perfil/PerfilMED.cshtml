@model ProyectoClinica.Models.IndexViewModel

@{
    ViewBag.Title = "Perfil";
}

<link href="~/CSS/Perfil.css" rel="stylesheet" />

<div style="text-align: center; margin-bottom: 20px;">
    <a href="@Url.Action("IndDOC", "Cita")" class="custom-btn">
        <i class="fas fa-home"></i> Inicio
    </a>
</div>

<main aria-labelledby="title" class="profile-container">
    <!-- Enlace a Font Awesome para los íconos -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <h2 id="title">@ViewBag.Title</h2>
    <p class="text-success">@ViewBag.StatusMessage</p>

    @if (!string.IsNullOrEmpty(Model.Imagen))
    {
        <img src="@Model.Imagen" alt="Perfil" class="profile-pic" />
    }
    else
    {
        <img src="~/Imagenes/PerfilPorDefecto.jpg" alt="Perfil" class="profile-pic" />
    }

    <div class="profile-details">
        <h2 class="profile-name">@Model.Nombre @Model.Apellido</h2>
        <p class="profile-email">@Model.Email</p>
    </div>

    <div class="profile-sections">
        <div class="section">
           
            <hr />

            <div class="section">
                <h3>Historial de citas</h3>
                <ul>
                    <li>
                        <a class="nav-link" id="btnNotificaciones" href="@Url.Action("DOCHCita", "Perfil")">
                            <i class="fas fa-calendar-check"></i> Historial de citas
                        </a>
                    </li>
                </ul>
            </div>
            <hr />

            <div class="section">
                <h3>Solicitud de recetas</h3>
                <ul>
                    <li>
                        <a class="nav-link" id="btnNotificaciones" href="@Url.Action("ConcenReceta", "Perfil")">
                            <i class="fas fa-file-medical"></i> Aprobación de Receta
                        </a>
                    </li>
                </ul>
            </div>
            <hr />


            <div class="section">
                <h3>General</h3>
                <ul>

                    @*Cierre se sesión*@
                    <li>
                        <a class="nav-link" href="javascript:document.getElementById('logoutForm').submit()">
                            <i class="fas fa-sign-out-alt"></i> Cerrar sesión
                        </a>
                    </li>

                    <form id="logoutForm" action="@Url.Action("Logout", "Account")" method="post" style="display:none;">
                        @Html.AntiForgeryToken()
                    </form>

                </ul>
            </div>
        </div>
</main>
