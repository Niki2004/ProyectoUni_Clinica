<!--@model IEnumerable<ProyectoClinica.Models.Cita>

@{
    ViewBag.Title = "Historial de Expedientes";
}

<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f5f5f5;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        background: #fff;
        margin-top: 20px;
    }

    thead {
        background-color: #3bb4bc;
        color: #fff;
    }

    th, td {
        padding: 10px;
        text-align: left;
        border: 1px solid #ddd;
    }

    tbody tr:hover {
        background-color: #f1f1f1;
    }

    .custom-btn {
        background-color: #0b7b82 !important;
        color: white !important;
        border: none;
        padding: 8px 12px;
        border-radius: 5px;
        font-size: 14px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 5px;
        cursor: pointer;
    }

        .custom-btn:hover {
            background-color: #09686e !important;
            text-decoration: none;
        }
</style>

<center>
    <a href="@Url.Action("MdExpedientes", "Expedientes")" class="btn custom-btn">
        <i class="fas fa-home"></i> Inicio
    </a>
</center>

<br />
<center><h2>Expedientes</h2></center>
<br />

<table id="expedientesTable" class="table">
    <thead>
        <tr>
            <th>Médico</th>
            <th>Paciente</th>
            <th>Estado</th>
            <th>Hora</th>
            <th>Complicaciones</th>
            <th>Síntomas</th>
            <th>Fecha</th>
            <th>Modalidad</th>
        </tr>
    </thead>-->
    @*<tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Medico.Nombre</td>
                <td>@item.Nombre_Paciente</td>
                <td>@item.Estado_Asistencia</td>
                <td>@item.Hora_cita</td>
                <td>@item.Descripcion_Complicaciones</td>
                <td>@item.Sintomas</td>
                <td>@item.Fecha_Cita.ToString("dd/MM/yyyy")</td>
                <td>@item.Modalidad</td>
            </tr>
        }
    </tbody>*@
<!--</table>

<br />
<center>
    <button class="btn custom-btn" onclick="descargarPDF()">
        <i class="fas fa-file-pdf"></i> Descargar PDF
    </button>

    <button class="btn custom-btn" onclick="autenticarYSubirPDF()">
        <i class="fas fa-cloud-upload-alt"></i> Guardar en Google Drive
    </button>
</center>-->

<!-- Cargar jsPDF y autoTable -->
<!--<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.25/jspdf.plugin.autotable.min.js"></script>
<script src="https://apis.google.com/js/api.js"></script>

<script>
    function descargarPDF() {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();

        doc.setFont("helvetica", "bold");
        doc.setFontSize(16);
        doc.text("Historial de Expedientes", 70, 15);

        doc.autoTable({
            startY: 25,
            html: '#expedientesTable',
            theme: 'grid',
            styles: { fontSize: 10, cellPadding: 3 },
            headStyles: { fillColor: [59, 180, 188] },
            alternateRowStyles: { fillColor: [240, 240, 240] },
            margin: { top: 20 }
        });

        doc.save("Historial_Expedientes.pdf");
    }

    const CLIENT_ID = "TU_CLIENT_ID";  // Reemplaza con tu CLIENT_ID de Google Cloud
    const API_KEY = "TU_API_KEY";      // Reemplaza con tu API_KEY de Google Cloud
    const DISCOVERY_DOCS = ["https://www.googleapis.com/discovery/v1/apis/drive/v3/rest"];
    const SCOPES = "https://www.googleapis.com/auth/drive.file";

    function autenticarYSubirPDF() {
        gapi.load("client:auth2", async () => {
            await gapi.client.init({
                apiKey: API_KEY,
                clientId: CLIENT_ID,
                discoveryDocs: DISCOVERY_DOCS,
                scope: SCOPES
            });

            const authInstance = gapi.auth2.getAuthInstance();
            authInstance.signIn().then(() => {
                generarYSubirPDF();
            });
        });
    }

    function generarYSubirPDF() {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();
        doc.setFont("helvetica", "bold");
        doc.setFontSize(16);
        doc.text("Historial de Expedientes", 70, 15);

        doc.autoTable({
            startY: 25,
            html: '#expedientesTable',
            theme: 'grid',
            styles: { fontSize: 10, cellPadding: 3 },
            headStyles: { fillColor: [59, 180, 188] },
            alternateRowStyles: { fillColor: [240, 240, 240] },
            margin: { top: 20 }
        });

        const pdfBlob = doc.output("blob");

        const metadata = {
            name: "Historial_Expedientes.pdf",
            mimeType: "application/pdf"
        };

        const formData = new FormData();
        formData.append("metadata", new Blob([JSON.stringify(metadata)], { type: "application/json" }));
        formData.append("file", pdfBlob);

        gapi.client.request({
            path: "https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart",
            method: "POST",
            body: formData,
            headers: {
                "Authorization": "Bearer " + gapi.auth.getToken().access_token
            }
        }).then((response) => {
            alert("Archivo subido a Google Drive correctamente");
        }).catch((error) => {
            console.error("Error al subir a Drive:", error);
        });
    }
</script>-->
