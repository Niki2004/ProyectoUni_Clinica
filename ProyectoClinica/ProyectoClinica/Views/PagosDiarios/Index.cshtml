@model IEnumerable<ProyectoClinica.Models.Pagos_Diarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <label>Id de Contabilidad</label>
        </th>
        <th>
            <label>Id del Empleado</label>
        </th>
        <th>
            <label>Fecha de Pago</label>
        </th>
        <th>
            <label>Monto</label>
        </th>
        <th>
            <label>Metodo del Pago</label>
        </th>
        <th>
            <label>Estado del Pago</label>
        </th>
        <th>
            <label>Observaciones</label>
        </th>
        <th>
            <label>Fecha de Registro</label>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Contabilidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Empleado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_Pago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Metodo_Pago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado_Pago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observaciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_Registro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Pago_Diario }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Pago_Diario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Pago_Diario })
        </td>
    </tr>
}

</table>
