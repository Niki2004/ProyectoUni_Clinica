@model IEnumerable<ProyectoClinica.Models.Servicios_Brindados>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Servicio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Servicio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_ServicioBrindado }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_ServicioBrindado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_ServicioBrindado })
        </td>
    </tr>
}

</table>
